/** Distance range for route filtering (km) */
export interface DistanceRange {
  min?: number;
  max?: number;
}

/** Elevation range for route filtering (m) */
export interface ElevationRange {
  min?: number;
  max?: number;
}
